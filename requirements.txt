streamlit==1.57.0
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
statsmodels>=0.14
scipy>=1.10
openpyxl>=3.1
# Authentication: Google sign-in via Streamlit's native OIDC (st.login),
# which requires Authlib. No password library is needed any more.
Authlib>=1.3.2
PyYAML>=6.0
# Database persistence (for free hosting with an ephemeral filesystem):
psycopg2-binary>=2.9
# Dashboard nav + PDF reports:
streamlit-option-menu>=0.3.12
reportlab>=4.0
